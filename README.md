# Gio Franz Magracia — Resume Site

Animated personal resume (Next.js App Router + TypeScript + Tailwind CSS + Framer Motion).

## Dev

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Content is sourced from the master resume. PMP status: **Eligible** (not certified).


## Notes

`package-lock.json` omitted from GitHub push due to size; run `npm install` locally.
